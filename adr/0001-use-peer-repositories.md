---
status: accepted
---

# Use peer repositories

VirtuJudge will use peer `Docs`, `Frontend`, `Backend`, and `AI-ML` repositories rather than a nested Git tree or daily-development submodules. The teams and release needs differ, while reviewed schemas and generated contract snapshots provide a safer coupling mechanism than repository nesting; local orchestration may clone the peers side by side.
